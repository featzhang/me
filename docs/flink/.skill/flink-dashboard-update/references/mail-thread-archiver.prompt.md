# Note

This file is an **identical copy** of `docs/flink/mail-thread-archiver.prompt.md` from the project root, snapshotted here so the skill stays self-contained when the source file is moved.

When producing a richer bilingual `dissuss/<thread_id>.html` (replacing the batch stub created by `_build.py`), feed this prompt plus the thread URL to a downstream agent and let it overwrite the file in place.

For day-to-day batch generation, **do not load this file** — `scripts/_build.py` already implements the English-only batch variant.

---

(The full prompt body lives at `docs/flink/mail-thread-archiver.prompt.md` — read that file directly when needed; do not duplicate ~200 lines here.)
